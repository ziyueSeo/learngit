第一步：实现把news.txt中的单词输入到数组当中
        建立一个char的二维数组，将每一个字母都放入数组中，遇到非字母的字符，就输出\0，变成一个相当于一维字符串数组的数组。
第二步：实现字符串比较，任何一个字符串都要与后面的所有字符串相比较，如果两个字符串相同，
        则相应的计数器加一（这里要建立一个与上一步的二维数组的第一维相同大小的int型数组，用于计数）
第三步：将计数数组按由大到小的方式排序，同时二维数组也按同样的方式排序，用于找到出现次数前十的数组